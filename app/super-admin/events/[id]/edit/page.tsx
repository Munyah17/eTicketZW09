// Thin re-export — same page component as @/app/admin/events/[id]/edit/page,
// reused so this tool lives inside this dashboard's own shell/URL tree
// instead of a separate portal.
export { default } from "@/app/admin/events/[id]/edit/page";
