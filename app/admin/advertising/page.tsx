// Thin re-export — same page component as @/app/creator/advertising/page, reused so this
// organizing tool lives inside this dashboard's own shell/URL tree
// instead of a separate portal. See docs: dashboard merge plan.
export { default } from "@/app/creator/advertising/page";
